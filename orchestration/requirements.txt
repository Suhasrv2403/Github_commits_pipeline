# Extra Python packages installed into the Airflow image (orchestration/Dockerfile),
# on top of the apache/airflow base image, so DAG tasks can call dbt/boto3/etc.
apache-airflow-providers-snowflake
dbt-snowflake
pandas
boto3
requests
pyyaml